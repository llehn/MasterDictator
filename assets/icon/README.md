# App icon

The Master Dictator icon: a leader behind a burning microphone, with a crowd in front of him. Everything except the microphone is misty steel blue, and the microphone is the only fire in the picture.

The same scene comes in two levels of detail. Small sizes need a shape you can recognise at a glance, and large sizes need detail.

## Files

| File | What it is | Use it for |
|---|---|---|
| `icon_small.svg` | 108×108, the Android adaptive-icon grid. Heavy dark outline on the microphone, little detail | Launcher icon, notification-sized and website favicon-sized uses: anything shown at roughly 128 px or smaller |
| `icon_large.svg` | 512×512, full detail: podium, clouds, a larger crowd, a studio microphone with a mesh grille | Play Store listing icon and anything shown larger than about 128 px |

In `icon_small.svg`, launchers only show the centre 72×72 and cut it to their own shape (circle, squircle, rounded square). Keep anything important inside the centre circle of diameter 66.

Both files use SVG filters (blur, grain, rim light). Android's VectorDrawable cannot express these, so the Android and Play Store assets have to be exported as PNGs.

## Changing the artwork

Don't edit the SVGs by hand. They are generated by `tools/icon/generate.js`:

```
node tools/icon/generate.js
```

This rewrites both SVGs in this folder. It needs Node.js.
